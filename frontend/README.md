# SkilledUp React Project (Complete)

This is a full React (Vite) starter project scaffold for the SkilledUp website.
Run:
1. npm install
2. npm run dev

Tailwind is pre-configured (tailwind.config.cjs & postcss.config.cjs).
